%h2.text-primary Se connecter à Sarce Trainer
= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
  .field
    %h4= f.label :email, class: 'label label-info'
    = f.email_field :email, class: 'form-control', autofocus: true
  .field
    %h4= f.label :password, class: 'label label-info'
    = f.password_field :password, class: 'form-control', autocomplete: 'off'
  - if devise_mapping.rememberable?
    .field
      = f.check_box :remember_me
      = f.label :remember_me, class: 'label label-default'
  .actions
    = f.submit 'Connexion', class: 'btn btn-primary'
= render "devise/shared/links"
